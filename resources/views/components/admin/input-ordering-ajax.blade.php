<div>
    <!-- Simplicity is an acquired taste. - Katharine Gerould -->
    <input type="number" class="form-control ordering" data-url="{{ $link }}" id="ordering-{{ $id }}"
        name="ordering" value="{{ $ordering }}" style="position:relative">
</div>
